[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "redis-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "upstash-redis>=0.15.0",
]

[tool.setuptools.packages.find]
include = ["redis_service*"]
