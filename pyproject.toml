# ./pyproject.toml
[project]
name = "kapex-ai"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "services/database",
    "backend",
    "worker",
]

[dependency-groups]
dev = [
    "fastapi[standard]>=0.139.2",
    "prisma>=0.15.0",
]
